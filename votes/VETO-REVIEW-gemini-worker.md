# VETO-REVIEW
## Teammate: gemini-worker
1. FIX — 다중 세션/팀 환경에서 잘못된 통신 라우팅(엉뚱한 outbox에 메시지 전송)을 유발할 수 있는 치명적인 버그입니다. 현재 실행 중인 프로세스의 소속 팀을 정확히 식별할 수 있는 수단이 필요합니다.
2. FIX — 일관성 없는 하드코딩된 파라미터는 유지보수를 어렵게 하고, 향후 기능 확장 시 버그의 원인이 됩니다.
3. FIX — 자동 생성 파일(`GEMINI.md`, `AGENTS.md`) 상단에 경고 주석이 없으면, 사용자가 실수로 파일을 직접 수정하여 변경사항이 유실될 위험이 높습니다.
4. FIX — 새로운 CLI(Codex) 지원 및 핵심 플래그(`-g`, `-T`) 기능은 사용자 경험에 직결되므로 반드시 README에 명시되어야 합니다.
5. FIX — 상태 파일에 잘못된 값(`model` 대신 `cli_cmd`)이 기록되면 모니터링이나 다른 연계 스크립트에서 오류가 발생할 수 있습니다.
6. FIX — 에이전트 종료 시 `config.json`의 상태가 동기화되지 않으면, 리드 에이전트나 UI가 해당 에이전트를 여전히 활성 상태로 오인할 수 있습니다.
7. SKIP — `bridge-mcp-server.js`는 `node`로 직접 실행되는 스탠드얼론 스크립트 특성이 강하므로, 패키지 매니저의 `scripts` 필드 추가가 필수적이지는 않습니다.
8. SKIP — 주석에 명시된 바와 같이 세션 활성화를 보장하기 위한 의도적인 트릭(워크어라운드)이라면, 완벽한 대체안이 나오기 전까지는 유지해도 무방합니다.
9. SKIP — 해당 없음.
